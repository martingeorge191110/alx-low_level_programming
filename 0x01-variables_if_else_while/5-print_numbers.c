#include <stdio.h>

/**
 * main - Entry point
 *
 * Description: print digit numbers form 0 to 9
 *
 * Return: 0 (Success)
*/

int main(void)
{
	for (int i = 0; i < 10; i++)
	{
		printf("%i", i);
	}
	putchar('\n');

	return (0);
}
