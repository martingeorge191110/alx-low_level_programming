#include <stdio.h>

/**
 * main - prints its name, followed by a new line.
 * @argc: argument counter
 * @argv: argument value
 *
 * Return: (0) Succes
 */

int main(int argc, char *argv[])
{
	(void)argc;
	printf("%s\n", argv[0]);

	return (0);
}
